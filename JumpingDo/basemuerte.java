import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class paredmuerte here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class basemuerte extends Actor
{
    /**
     * Act - do whatever the paredmuerte wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        GreenfootImage image = new GreenfootImage(512, 10);
        setImage(image);
    }
}
